{
  "name": "pushtracker",
  "version": "0.1.0",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "start": "bun run src/server.ts",
    "test": "bun test"
  },
  "dependencies": {
    "luxon": "^3.6.0"
  },
  "devDependencies": {
    "@types/luxon": "^3.4.0"
  }
}
